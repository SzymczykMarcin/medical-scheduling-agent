# Choose one runtime profile and copy it to .env:
#
# Local model server:
#   copy .env.example.local-ollama .env
#
# Cloud Run backend calling model services:
#   copy .env.example.cloud-run .env
#
# This file intentionally contains no machine-specific model paths.
